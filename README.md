# Интерактивные сериалы – финальный прототип

## Установка и запуск
1. Установите Node.js и PostgreSQL.
2. Создайте БД и выполните `create_tables.sql`.
3. В папке `server` создайте `.env` с вашими данными.
4. Установите зависимости: `npm install`
5. Запустите: `npm start`
6. Откройте http://localhost:3000

## Тестовый вход админа
- Логин: `admin`
- Пароль: `admin123`

## Структура
- `/` – главная страница сериала
- `/player.html?episodeId=1` – плеер
- `/admin-login.html` – вход админа
- `/admin.html` – панель управления (заглушка)

## Дальнейшие шаги
- Реализовать полноценную админ-панель (CRUD сериалов, серий, глав, переходов).
- Добавить редактор областей на видео.
- Реализовать предпросмотр и публикацию.

- 🔍 Как читать статистику
После обновления откройте:

text
https://za_kray.onrender.com/api/stats?key=series_stats_key
Ответ будет такой:

json
{
  "total": 128,
  "unique": 47,
  "lastUpdated": "2026-09-10T18:32:11.000Z"
}
total — сколько раз вообще запускали любую серию (суммарно).

unique — сколько разных браузеров заходило.

lastUpdated — когда последний раз обновлялся файл.
